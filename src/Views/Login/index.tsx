import { Text } from "react-native";

import { StyledView } from "./styles";

export default function Login(): JSX.Element {
  return (
    <StyledView>
      <Text>About</Text>
    </StyledView>
  );
}
